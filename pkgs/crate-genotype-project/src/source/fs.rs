use crate::prelude::internal::*;

/// File system project source. It uses the file system to read project files.
pub struct GtProjectSourceFs {}

impl GtProjectSource for GtProjectSourceFs {
    fn glob() -> () {
        todo!()
    }

    fn search() -> () {
        todo!()
    }

    fn read() -> () {
        todo!()
    }
}
