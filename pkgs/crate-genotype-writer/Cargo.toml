[package]
name = "genotype_writer"
version = "0.15.0"
edition = "2024"
description = "Genotype language writer crate"
authors = ["Sasha Koss <koss@nocorp.me>"]
license = "MIT"
repository = "https://github.com/kossnocorp/genotype"

[dependencies]
genotype_lang_core_project = { version = "0.15.0", path = "../crate-genotype-lang-core-project" }
genotype_config = { version = "0.15.0", path = "../crate-genotype-config" }
genotype_path = { version = "0.15.0", path = "../crate-genotype-path" }
[dev-dependencies]
pretty_assertions = "1.4.1"
insta = { version = "1.46.3", features = ["ron"] }
