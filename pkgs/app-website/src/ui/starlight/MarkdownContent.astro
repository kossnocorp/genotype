---
import Default from "@astrojs/starlight/components/MarkdownContent.astro";
import { isBareAstroPage } from "./utils";

const isBare = isBareAstroPage(Astro.locals);
---

{
  isBare ? (
    <slot />
  ) : (
    <Default {...Astro.props}>
      <slot />
    </Default>
  )
}
