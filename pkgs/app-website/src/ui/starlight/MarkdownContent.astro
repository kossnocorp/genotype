---
import Default from "@astrojs/starlight/components/MarkdownContent.astro";

// @ts-expect-error: See `env.d.ts`
const isBare = Astro.locals.starlightRoute.entry.data.bare;
---

{
  isBare ? (
    <slot />
  ) : (
    <Default {...Astro.props}>
      <slot />
    </Default>
  )
}
