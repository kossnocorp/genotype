// Generate Effect v4 schemas and inferred TypeScript types.
// Use Schema.decodeUnknownSync(User)(input) to validate untrusted data at runtime.

Role: "admin" | "member"

User: {
  name: string,
  role: Role,
  tags: [string],
  email?: string,
}
