// Switch between "Python Legacy" and "Python Modern" to compare
// the generated Python code.

UserId: string

User: {
  id: UserId,
  tags: [string],
  metadata: { []: string },
  contact: string | null,
  nickname?: string,
}

Page<Item>: {
  items: [Item],
  total: int,
}

Users: Page<User>
