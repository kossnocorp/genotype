[package]
edition = "2021"
description = "Genotype language JSON types"
license = "MIT"
name = "genotype_json_types"
version = "0.1.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
literals = "0.1"

