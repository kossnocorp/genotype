[package]
edition = "2021"
description = "Genotype language JSON types"
license = "MIT"
name = "genotype_json_types"
version = "0.3.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
litty = "0.2"

